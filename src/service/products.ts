/**
 * 제품 리스트를 반환하는 함수
 */
export function getProducts() {
  return ["shirt", "pant", "skirt", "shoes", "dress"];
}

/**
 * 제품 id를 인자로 받아 제품의 객체를 반환하는 함수 (미완성)
 */
export function getProduct(id: string) {
  return "shirt";
}
